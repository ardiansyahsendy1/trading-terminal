# Copy this file to .env.local for local-only settings.
# Do not commit real API keys or secrets.

# OPENAI_API_KEY is reserved for future server-side or local maintainer automation.
# Browser-delivered Vite variables are public and must not contain secrets.
OPENAI_API_KEY=
